import styled from "styled-components";

import { Button } from "../common";

const ButtonContainer = styled(Button)``;

const WalletButton = () => {
  return <ButtonContainer>Connect Wallet</ButtonContainer>;
};

export default WalletButton;
